#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
    _puts_recursion("Puts with recursion");
    _puts_recursion("talk is cheap, show me the code");
    return (0);
}
