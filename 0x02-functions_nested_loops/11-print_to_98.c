#include "main.h"

/*
 *:
 */

void print_to_98(int n)
{
	int b;
	if (a >= 98)
	{
		for (b = n; b >= 98; b--)
		{
			printf("%d, ",b);
		}
	}
	else
	{
		for (b = n; b <= 98; b++)
		{
			printf("%d, ",b);
		}
